using System.Data;
using System.Windows.Forms;

namespace MHM.Accountancy.Business.Infrastructure
{
    public static partial class Helper
    {
        //static public T GetBindingRow<T>(this BindingSource bindingSource)
        //    where T : DataRow
        //{
        //    if (bindingSource == null || bindingSource.Position < 0 || bindingSource.Current == null)
        //        return null;

        //    return (bindingSource.Current as DataRowView).Row as T;
        //}
    }
}
